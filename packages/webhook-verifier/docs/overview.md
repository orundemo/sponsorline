# webhook-verifier

Shared workspace package.

A shared package of the sponsorline workspace, consumed by the components
below at build time.

## Depended on by

- **cli** — First-class TypeScript CLI for the SponsorLine control plane API
